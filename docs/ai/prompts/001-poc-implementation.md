# POC Implementation Prompt

The authoritative prompt for this implementation is `.aiassistant/rules/rules.md`.
It was copied into the repository prompt log by reference to avoid duplicating personal
environment metadata. The implementation follows the document-processing pipeline brief:
FastAPI, Temporal Python SDK, PostgreSQL with async SQLAlchemy, Alembic, MinIO, uv,
pytest, deterministic mock extractor/classifier adapters, recovery behavior, full rerun
semantics, and reviewer-oriented documentation.
